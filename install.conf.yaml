- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.config: .config
#    ~/.vim: vim
#    ~/.vimrc: vimrc

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

#- shell:
#    - [git submodule update --init --recursive, Installing submodules]
